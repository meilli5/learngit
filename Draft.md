$\textstyle \lim_{ n \to \infty }\sum_{j=1}^{n}\frac{1}{\sqrt{n^2+j  }}$
Let $(a_{n})$ be a sequence converging to $a$, prove that the sequence $\left(\frac{\sum_{j=1}^n a_{j}}{n}\right)$ is convergent with the limit $a$.
$f(x)=\frac{x^2}{1+x^2}$
$f(x)=O(\phi(x))$
$x \in ( -\infty,\sqrt[3]{2}]\cup[0,\infty),f(x)\geq 0$
$x_0 \in \bar{I}$
$\Omega$
$if \lim_{x \to 0} \frac{|f(x)|}{|\phi (x)|} =0, then \quad f(x) = o(\phi(x))$
$D(x)=\begin{cases} & 1,x \in \mathbb{Q}\\ & 0,x \notin \mathbb{Q} \end{cases}$

$O(f(x))+O(g(x))=O(|f(x)|+|g(x)|)\quad as \quad x \to x_{0}$
$O(x^3)=O(x^2) \quad as \quad x \to x_{0}$
$O(f(x))o(g(x))=o(f(x)g(x))\quad as\quad x\to x_{0}$
$O(O(f(x)))=O(f(x))\quad as \quad x\to x_{0}$
$\underline{\lim} \left \{\frac{1}{n},n \in \mathbb{Z} \setminus \{ 0 \}\right\}$
$1^{*}$
$\mathrm{1}$
$\mathrm{A}$
$\mathbf{dadfadafvxc}$
$\hat{a} \bar{b} \dot{a} \ddot{a}$
$\mathbf{a}$
$\mathbf{a}$
$\boldsymbol{\omega}$
$x_{i} x_{n} x_{j} \infty \sum \prod \dots \nabla \cdot \times \parallel$
$\equiv$
$\sim$
$\simeq$
$\emptyset$
$\propto$
$\leftrightarrow \mapsto$
$\cap \cup \in \not\in \subseteq \supseteq$
$\exists$
$\mathbb{Q}$
$\oint$
$\iiint \int_{a}^{b}  \, dx$
$\int_{0}^{\infty}  \, dx$
$\text{If there exists a subsequence of }(a_{n}) \text{ that converges to a, then a is an accumulation point of }(a_{n})$

$\text{How to prove }f(x)=x^2\text{ is not uniformly continuous on }\mathbb{R}.$

$\text{Let } (a_{n}) \text{ be a real Cauchy sequence and }f:I \to \mathbb{R},I \subset \mathbb{R} \text{ an interval, be uniformly continuous. Prove that}(f(a_{n}))\text{ is again a Cauchy sequence.}$
$\rho$
$\text{Let }a<b\text{ and }f:[a,b]\to \mathbb{R}\text{ be a continuous function. How to prove }f\text{ is bounded?}$
$\text{Uniformly continuous function }f:I \to \mathbb{R}\text{ defined on bounded interval }(a,b)\text{ is bounded}$
$\text{Given a bounded interval }(a,b)\text{, }f \text{ is uniformly continuous on }(a,b)\text{ if and only if for all Cauchy sequence }{x_{n}}\text{ in the interval }(a,b)\text{, the sequence }{f(x_{n})}\text{ is also a Cauchy sequence}$
为什么$\text{if }\lim inf A \text{ exists, then }infA\text{ exists}$

$\frac{\sqrt{ x^4+x }}{3x+5}$的渐近线

Let *I* be an open interval and let $f:I\to \mathbb{R}\text{ be differentiable and strictly monotonic. Then the inverse map }f^{-1}=g:f(I)\to I \text{exists and is differentiable at all points }y\in f(I)\text{ for which }f'(g(y))\ne0\text{. Furthermore, }g'(y)=\frac{1}{f'(g(y))}$. 
Suppose a continuous function $f:I\to \mathbb{R}$, $I=[1,2]$ and $\forall x\in [1,2),f(x)\geq 0$, how to prove that $f(2)\geq 0$?

how to prove $c_{0} \subset \ell^{\infty}$ as vector spaces?
Hi Professor Wei, I have two questions about the Project 2 Proposal: What is the format of the title of "Solution" part (How to write the project name)? And does this paragraph mean that the entire "solution" section is approximately 1-2 pages long?

Why $p \leq \underline{\lim}_{n \to \infty}\sqrt[n]{a_{n} }\leq \overline{\lim}_{n \to \infty}\sqrt[n]{ a_{n} }\leq p$ means that $\lim_{ n \to \infty }\sqrt[n]{a_{n}  }=p$ ?
Why the map $z \mapsto \bar{z}$ is a continuous map $\mathbb{C}\to \mathbb{C}$ ?

对于series $A=\sum_{k=0}^{\infty}(-1)^ka_{k}$, 如果$a_{k}$不收敛到0，是不是A就发散？


\***total_acceleration=$\sqrt{ a_{x}^2+ a_{y}^2+a_{z}^2 }$**


$sup_{x\in \mathbb{R}}|\frac{\sin x}{x}|$
$f:\mathbb{R}\to \mathbb{R},f(x)=\frac{\sin x}{x}\quad(x\neq 0),f(0)=0$, 那么$sup_{x\in \mathbb{R}}|f(x)|$和$sup_{x\in \mathbb{R}}|f^{'}(x)|$分别是多少

对于积分$\int \tan xdx$，我能否用以下方式进行求解：
$p=\tan x,\quad x=\arctan p,\quad dx=d\arctan=\frac{dp}{1+p^2}$, 所以$$\int \tan xdx=\int \frac{p}{p^2+1}dp$$
设$u=p^2+1,\quad du=2pdp,$ $$
\int \frac{p}{p^2+1}dp=\int\frac{1}{2u}du=\frac{1}{2}\ln (\tan^2x+1)+C
$$



Heroes own empathy and integrity with themselves so they can identify more accurately some of the current problems that exist, whether obvious or potential. And when they are going to solve the problems, they all, to some degrees, will encounter some risks and crises and bear some responsibilities because they need to take actions. And what sets heroes apart from other empathetic and just people lies in the bravery that enable them to take actions to further the welfare of another without expectation of reward, regardless of whether there exists negative consequences, risk or not.


$\frac{d}{dx}\left( \int_{0}^xf(\xi)d\xi \right)=f(x)$, because $f\in C(\mathbb{R})$, so $f$ is continuous, so $g(x)=\int_{0}^xf(\xi)d{\xi}$ is continuously differentiable. So $g$ is also continuous. 
So $\lim_{ x \to 0 }\int_{0}^xf(\xi)d\xi=\int_{0}^0f(\xi)d\xi=0$, so $$
\lim_{ x \to 0 }\frac{|\int_{0}^xf(\xi)d\xi|}{|f(x)|} =\frac{0}{|f(0)|}=0
$$
So $\int_{0}^xf(\xi)d\xi = o(f(x)) \quad as \quad x\to 0$


How to calculate the radius of convergence for the power serie $$
\sum_{n=1}^{\infty}\frac{x^{2n}}{3^{2n}-n}
$$
We have known that $$
\left | \int_{a}^b f \right |\leq |b-a|\sup_{x \in[a,b]}|f(x)|
$$
can we also have $$
\int_{a}^b |f|\geq |b-a|\inf_{x \in[a,b]}|f(x)|
$$
### $\mathbf{1.3}$    $\mathbf{Clarifications}$
There is actually an input $h$ for this defivative function. Do not confuse "defivative at a point" with "function that gives derivative". At a certain point $x$, for $f(x)=x^4$, we have $f'(x)=4x^3$, which is a number in $\mathbb{R}$. Using our notation for $L_{x}\text{(or } f'(x))$, we can express $L_{x}$ as $$
L_{x}(\cdot)=4x^3(\cdot),
$$meaning $L_{x}(h)=4x^3\cdot h$. The variable of $L_{x}$ is not $x$, so $L_{x}$ is linear in its input.

$$
(f^{-1})'(x)=\frac{1}{f'(y)|_{y=f^{-1}(x)}}
$$
